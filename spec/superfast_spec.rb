# spec/superfast_spec.rb
RSpec.describe Superfast do
  it "has a version number" do
    expect(Superfast::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
