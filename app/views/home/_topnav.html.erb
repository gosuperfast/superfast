<nav class="bg-gray-800">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4">
            <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
            <!--
            <a href="#" class="rounded-md bg-gray-900 px-3 py-2 text-sm font-medium text-white" aria-current="page">Product</a>
            <a href="#" class="rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Explore</a>
            <a href="#" class="rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Resources</a>
            <a href="#" class="rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Pricing</a>
            -->
            <!--Turbo rails-->
            <%= link_to 'Product', product_path, class: 'rounded-md bg-gray-900 px-3 py-2 text-sm font-medium text-white', 'aria-current': 'page' %>
            <% if current_user.present? %>
              <%= link_to 'Sign Out', destroy_user_session_path, class: 'no-underline block p-2 text-gray-900 bg-white \
                                                                          hover:bg-gray-100 whitespace-nowrap focus:bg-gray-100',
                                                                data: { turbo_method: :delete } %>
            <% else %>
              <li><%= link_to "Sign in", new_user_session_path, class: 'no-underline block p-2 text-gray-900 bg-white \
                                                                          hover:bg-gray-100 whitespace-nowrap focus:bg-gray-100' %></li>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>